extends ../layout

block content
  h1 Login
  form(action="/login",method="post")
        .form-group
            label(for="email") Email
            input#email(type="text", class="form-control",name="email",value="abubakar0707@gmail.com")
        .form-group
            label(for="password") Password
            input#password(type="text", class="form-control",name="password",value="freakspy007")
        input(type="submit", class="btn btn-info" value="Register")
